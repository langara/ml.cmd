Clone this repo to github
rewrite all scripts from ~/bin as functions in cmd.py
Register this project on PyPi
REMEMBER:
    How to add new commands:
    1. add new commands to ml/cmd.py (change VERSION)
    2. update setup.py (change version)
    3. run: python ./setup.py develop --user
    4. test new commands
    5. commit changes to git and push.
